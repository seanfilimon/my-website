/**
 * Export GSAP plugins for easy importing
 */
export { ScrollTrigger } from "gsap/ScrollTrigger";
export { ScrollToPlugin } from "gsap/ScrollToPlugin";

// Note: ScrollSmoother requires GSAP membership
// If you have a GSAP membership, uncomment:
// export { ScrollSmoother } from "gsap/ScrollSmoother";

